//! Surrogate model — stub
//!
//! The full surrogate acquisition logic was migrated to glim-think.
//! This stub preserves the module boundary for compilation.

/// Placeholder surrogate config.
pub struct SurrogateConfig;

impl Default for SurrogateConfig {
    fn default() -> Self {
        Self
    }
}
