"""Pytest bootstrap for the neural-symbolic node tests.

The real ``neural_symbolic`` package is merged into this test package via
``__path__`` extension in ``__init__.py``, so no ``sys.path`` manipulation is
required here.
"""
